# _template

Copy this directory to `images/<image>/` when adding a new image.

Notes:

- Remove this file's contents and replace with image-specific documentation.
- Ensure `image.toml` reflects your intended `version` tagging behavior.

